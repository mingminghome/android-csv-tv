<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>
            <solid android:color="#FFFFFF"/> <!-- White background when focused -->
            <stroke android:width="2dp" android:color="#BDBDBD"/> <!-- Grey focus border -->
        </shape>
    </item>
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>
            <solid android:color="#CCCCCC"/>
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>
            <solid android:color="#2C2C2C"/> <!-- Dark grey background -->
            <stroke android:width="1dp" android:color="#44FFFFFF"/> <!-- Subtle white border -->
        </shape>
    </item>
</selector>
